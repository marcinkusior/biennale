<%= form_for([:admin, @event]) do |f| %>
  <% if @event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@event.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
      <% @event.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="row">
    <div class="col-xs-9">
      <div class="field ">
          <%= f.label :timedate, 'Data + miejsce' %><br>
        <%= f.cktext_area :timedate%>
        </div>
    </div>

    <div class="col-xs-9 col-xs-offset-1">
      <div class="field ">
          <%= f.label :description, 'opis' %><br>
        <%= f.cktext_area :description %>
      </div>
    </div>
  </div>

    <div class="row">
    <div class="col-xs-9">
      <div class="field ">
          <%= f.label :timedatePL, 'Data + miejsce PL' %><br>
        <%= f.cktext_area :timedatePL %>
        </div>
    </div>

    <div class="col-xs-9 col-xs-offset-1">
      <div class="field ">
          <%= f.label :descPL, 'opis PL' %><br>
        <%= f.cktext_area :descPL %>
      </div>
    </div>
  </div>

  <div class="field">
      <%= f.label :order, 'Kolejność' %><br>
    <%= f.number_field :order %>
    </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
