<%= form_for([:admin, @event]) do |f| %>
  <% if @event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@event.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
      <% @event.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="row">
    <div class="col-xs-9">
      <div class="field ">
          <%= f.label :timedate, 'Data + miejsce' %><br>
        <%= f.bootsy_area :timedate, class: 'form-control', rows: 8 %>
        </div>
    </div>
    
    <div class="col-xs-9 col-xs-offset-1">
      <div class="field ">
          <%= f.label :description, 'opis' %><br>
        <%= f.bootsy_area :description, class: 'form-control', rows: 8  %>
      </div>
    </div>
  </div>

    <div class="row">
    <div class="col-xs-9">
      <div class="field ">
          <%= f.label :timedatePL, 'Data + miejsce PL' %><br>
        <%= f.bootsy_area :timedatePL, class: 'form-control', rows: 8  %>
        </div>
    </div>
    
    <div class="col-xs-9 col-xs-offset-1">
      <div class="field ">
          <%= f.label :descPL, 'opis PL' %><br>
        <%= f.bootsy_area :descPL, class: 'form-control', rows: 8  %>
      </div>
    </div>
  </div>

  <div class="field">
      <%= f.label :order, 'Kolejność' %><br>
    <%= f.number_field :order %>
    </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
