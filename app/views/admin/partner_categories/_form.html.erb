<%= form_for([:admin, @partner_category]) do |f| %>
  <% if @partner_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@partner_category.errors.count, "error") %> prohibited this partner_category from being saved:</h2>

      <ul>
      <% @partner_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :namePl %><br>
    <%= f.text_field :namePl %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
