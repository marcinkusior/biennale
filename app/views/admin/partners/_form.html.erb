<%= form_for([:admin, @partner]) do |f| %>
  <% if @partner.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@partner.errors.count, "error") %> prohibited this partner from being saved:</h2>

      <ul>
      <% @partner.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
      <%= f.label :image, 'Zmień obraz:' %><br>
    <%= f.file_field :image %>
    </div>
    
    <div class="field">
      <%= f.label :kind, 'Typ:' %><br>
      <%= f.select :kind, [['Zwykły', 'normal'], ['Honorowy', 'honor'], ['Medialny', 'media']] %>
    </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
