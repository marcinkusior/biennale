<%= form_for([:admin, @contest_archive]) do |f| %>
  <% if @contest_archive.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@contest_archive.errors.count, "error") %> prohibited this contest_archive from being saved:</h2>

      <ul>
      <% @contest_archive.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
      <%= f.label :title, 'Tytuł' %><br>
    <%= f.text_field :title %>
    </div>
  <div class="field">
      <%= f.label :titlePL, 'Tytuł PLs' %><br>
    <%= f.text_field :titlePL %>
    </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
