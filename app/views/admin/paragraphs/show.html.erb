<p id="notice"><%= notice %></p>

<div class="row">
	<div class="col-md-6 col-md-offset-3">
		<p>
		  <strong>Body:</strong>
		  <%= raw( @paragraph.body ) %>
		</p>
	</div>
</div>

<p>
  <strong>Page:</strong>
  <%= @paragraph.page.title %>
</p>

<%= link_to 'Edit', edit_admin_paragraph_path(@paragraph) %> |
<%= link_to 'Back', admin_paragraphs_path %>
