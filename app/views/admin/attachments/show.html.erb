<p id="notice"><%= notice %></p>

<p>
  <strong>Src:</strong>
  <%= request.domain + @attachment.src.to_s %>
</p>

<p>
  <strong>Name:</strong>
  <%= @attachment.name %>
</p>

<%= link_to 'Edit', edit_admin_attachment_path(@attachment) %> |
<%= link_to 'Back', admin_attachments_path %>
